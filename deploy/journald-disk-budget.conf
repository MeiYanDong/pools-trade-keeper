# Host-level safety budget for persistent systemd journals.
# This is deliberately size-based: it retains recent diagnostics while preventing
# an unrelated log backlog from tripping the Shadow release disk-pressure gate.
[Journal]
SystemMaxUse=1G
SystemKeepFree=4G
